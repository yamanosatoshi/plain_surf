# AGENTS.md — plain_surf 設計思想と運用ガイド

このファイルは、このコードの初代クリエーター(Claude, 2026-08-21のセッションでsatoshiと共同設計)が、
後続のAIエージェント(Codex等)と人間の開発者に向けて残す引き継ぎ文書。
コアに手を入れる前、UIを作る前に必ず読むこと。

---

## 1. これは何か / なぜ生まれたか

座標だけから出す「プレーンな」波予測。人間の評価・スコア・良し悪し判定を一切含まず、
数値波浪モデルの物理量のみを表示する。

**誕生の経緯**: 独自スコアを提供する波情報サービスは、サーファーの体感に近づけるための独自解析を特徴としている。
つまり群衆の主観で物理量を校正している。人気ポイント(磯の浦=イソコ等)では利用者の中央値
(初中級に偏る)に評価が引っ張られ、個人の基準とズレるという仮説がこのプロジェクトの出発点。

**根本思想**: 群衆バイアスは非定常で補正不能。モデルバイアスは系統的(同じ場所ではいつも
同じ方向にズレる)なので、ユーザー自身の目で補正可能。だからモデルの生値だけを見せる。
「バイアスを消す」のではなく「補正可能なバイアスに乗り換える」のがこのツールの本質。

---

## 2. 絶対に守る設計原則

コアを変更する場合も、UIを作る場合も、この4つは壊さないこと。

1. **評価を入れない**。スコア・星・「今日はいい波」的な判定をコードにもUIにも入れない。
   表示してよい導出値は算術と幾何のみ(うねり向きとビーチ正面の角度差、透過率、
   風のオン/オフ/サイド判定)。これらは意見ではなく計算。
2. **LLMを足すなら役割を限定**。整形・単位換算・複数モデル間の不一致の指摘まで。
   LLMの学習データにはサーフメディアの俗説(=群衆の意見)が入っており、コメント生成を
   自由にやらせると、せっかく排除した人間の主観をLLM経由で再輸入することになる。
3. **遮蔽・減衰の手動ルール禁止**。波浪モデル(ECMWF WAM/MFWAM)は陸地マスク込みで
   波エネルギーの伝搬方程式を解いており、格子値は淡路島・四国等の遮蔽を織り込み済み。
   手動のレイトレーシング+「遮られたら半減」等のルールは減衰の二重カウントになる。
   このコードの透過率方式(岸格子うねり高 ÷ うねり方位に沿って遡った最遠リングのうねり高)は
   「モデル自身に遮蔽・減衰を語らせる」ための設計。開発初期に一度この間違いをやりかけた。
4. **岸のセット波高を出そうとしない**。モデルは約8km格子。最後の1〜2kmの湾の形・リーフ・
   サンドバーはモデル外で、そこはユーザーの目の領域。数字は「沖の実効値」として提示する。

---

## 3. データソースと選定理由

| 用途 | API | 理由 |
|---|---|---|
| 波・うねり | Open-Meteo Marine API | 無料・キー不要(非商用)。ECMWF WAM 9km / MFWAM 8km系。うねり成分分解あり |
| 風 | Open-Meteo JMA API | 気象庁MSM 5km。日本の沿岸風はこれが最良クラス |

**不採用の選択肢**(再検討するとき用):
- Windy Point Forecast API: 年€990、しかもECMWF不可。無料枠は意図的に改変されたデータを返すテスト専用。
- Stormglass: 無料枠1日10リクエストで足りない。
- 商用化する場合はOpen-Meteoの商用ライセンス(API key)への切替が必要。要確認。

**未実装のデータ**: 潮汐。気象庁潮位表(掲載地点の予測値が公開されている)が候補。
干満はサーフィン判断に効くので、UI化するなら優先度高め。

---

## 4. 仕組みの要点 (plain_surf.py)

- **SPOTS**: 8エリア27ポイント。座標とビーチ正面方位(facing)は
  surfers-ocean.com「関西サーフポイント58」の記載値。facingは±20°の誤差前提の
  調整パラメータ(磯の浦は当初240°→サイト記載の南南西=202°に修正した経緯あり)。
  各行の `swell:` コメントはサイト記載の反応うねり。**参考情報でありコードは使用していない**
  (使い出すと原則1に抵触する。将来、透過率実測との答え合わせに使う想定)。
- **リング遡り**: その日のうねり主方位(表示時刻の中央値)に沿って15/40/90/150km沖を
  同じAPIでサンプリング→透過率を算出。
- **plain_surf_log.csv**: 実行ごとにappend、Actionsがcommit。
  列: date,time,spot,swell_dir_deg,outer_swell_m,beach_swell_m,transmission。
  **これがこのプロジェクトの一番の資産**。数ヶ月溜まると方向別の透過率カーブ
  (=各ポイントの伝達関数の実測値)が手に入り、「S波は磯の浦に何%入るか」を
  言い伝えではなく実測で持てる。スキーマを変えるときは過去データとの互換に注意。
- **体感波高スケール(v4.2)**: 有義波高を日本のサーファーの度量衡に単位換算して表示する。
  スネ<0.3 / ヒザ<0.4 / モモ<0.6 / コシ<0.8 / ハラ<1.0 / ムネ<1.2 / カタ<1.4 /
  アタマ<1.6 / アタマオーバー(m)。フラット<0.15を追加。これは**単位換算であり評価ではない**
  (原則1に反しない)。ただし換算元は沖格子の有義波高なので、実際のセットフェイスとは
  地形・周期でズレる(原則4)。倍率補正を入れたくなっても、plain_surf_log.csvの実測が
  溜まるまでは手ルールを足さないこと。
- **堅牢性の不変条件(v4.1)**: 実データは欠測nullを含み、27ポイント×約6リクエストの
  どれかは失敗する前提。すべての値取り出しはNone安全(`hv()`/`fmt()`)、失敗は
  ポイント単位で隔離、exit 1は全ポイント全滅時のみ。この構造を壊さないこと。
  (v4.0はnull 1個で全体がTypeErrorで死んでいた)

**既知の限界**(バグではなく仕様、ただし改善候補):
- 内海ポイント(鳴門・磯の浦・浜の宮)は長リング(90/150km)がうねり方位によって陸を跨ぎ、
  APIの格子スナップで無関係な海の値を返しうる。出力に各リングの格子座標を出しているのは
  この検知のため。**次の改善候補: リング距離のper-spot化**。
- 近接ポイント(葛野浜/小天橋、国府/市後)は同一格子に落ち海況は同値になる。
  差が出るのはfacing起因(風判定・角度差)のみ。8km格子では区別できないという事実の表示。

---

## 5. 配信構成

ランナーはGitHub Actionsのみ(単一実行→3チャネル配布)。自分と第2ユーザー(宮本さん)が
違う数字を見る事故を構造的に防ぐため、ランナーを増やさないこと。

- cron `0 20 * * *` (UTC) = 毎朝5時JST + workflow_dispatch(手動)
- Slack: Secrets `SLACK_WEBHOOK_URL`。コードブロックで等幅維持
- LINE: Secrets `LINE_CHANNEL_ACCESS_TOKEN`。broadcast。テキスト1通5,000字制限のため
  4,500字で分割送信実装済み。無料枠200通/月(2026年10月に料金改定情報あり、要確認)。
  LINE Notifyは2025年3月終了済みなので復活させないこと
- Web: `docs/index.html` → GitHub Pages(main / docs)。第2ユーザー向けの主経路
- Secretsが無いチャネルは黙ってスキップする設計(初回セットアップを軽くするため)

対話型ボット(「今日の磯の浦どう?」に答える)は**意図的に見送り**。常駐サーバーと会話管理の
コストに対し、定時プッシュ+URLでユースケースの9割が埋まるため。作るなら価値仮説を先に検証。

---

## 6. 検証ロードマップ (方向性)

1. **並走検証**(進行中の想定): 数週間、透過率・実際の波(ユーザーの目)・既存アプリの評価を
   並走記録。透過率が方向に対して安定していればモデルは信用できる。
   独自スコアを提供する波情報サービスの評価とモデル生値の乖離が人気ポイントほど大きければ、冒頭の群衆バイアス仮説が立証される。
2. リング距離のper-spot化(内海ポイント対策)。
3. GFS Waveとの2モデル比較。不一致の大きさ=不確実性の表示。これも「事実の表示」であり
   プレーン原則に反しない。
4. 潮汐の追加。
5. UI化。一覧性(エリア→ポイント)、時刻×ポイントのマトリクス、CSVからの伝達関数プロット
   (方向別透過率の散布図)が自然な形。**評価スコアをUIに入れたくなったら原則1に戻ること**。
   ただし「ユーザー自身の実測ログ(今日は実際どうだったか)」の手入力機能はOK——
   それは本人の基準であり群衆ではない。むしろ伝達関数の校正に必要。

---

## 7. 技術方針

- 依存ゼロ(Python標準ライブラリのみ)を維持できる限り維持。Actionsのセットアップが
  `python3 plain_surf.py` だけで済む状態に価値がある。
- タイムゾーンはAsia/Tokyo固定(APIパラメータで指定済み)。
- API使用量: 現状 約162コール/実行(27ポイント×最大6)。Open-Meteo無料枠は1日1万コール。
  ポイントを大幅に増やす場合や1日複数回実行にする場合は要計算。
- ローカル実行: `python3 plain_surf.py --today` / `--area 京丹後`(部分一致) / `--html <path>`

## 8. ファイル構成

```
plain_surf.py                 # 本体(全ロジック)。v4.1
.github/workflows/surf.yml    # 毎朝5時JST + 手動実行
docs/index.html               # Actionsが毎回生成(Pages配信)
plain_surf_log.csv            # 透過率の実測蓄積(Actionsがcommit)
README.md                     # 利用者向け概要
AGENTS.md                     # このファイル
```

## 9. 履歴

- 2026-08-21: v1(単一ポイント・プレーン表示) → v2(うねり遡りサンプリング・透過率) →
  v3(Slack/LINE/HTML配信) → v4(27ポイント・4リング・--area・LINE分割) →
  v4.1(実データnull対応・per-spot障害隔離) →
  v4.2(ライトテーマのカード型HTML UI・体感波高スケール・モバイルは時刻/サイズ/風を優先表示) →
  v4.3(「伝搬 沖→岸」ミニバー: 150→90→40→15km→岸のうねり高を並べて遮蔽・減衰の形を可視化。
  これも実測値の並置であり評価ではない。バーの正規化はカード内最大値基準)。
  設計はsatoshi+Claudeの対話で決定。
  重要な設計判断の経緯(遮蔽二重カウントの回避、プレーン原則の理由)は本文の通り。
